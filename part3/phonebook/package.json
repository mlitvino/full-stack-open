{
  "name": "phonebook",
  "version": "0.0.1",
  "description": "Phonebook app with frontend and backend",
  "scripts": {
    "build:ui": "cd backend && npm run build:ui",
    "deploy:commit": "git add . && git commit -m 'deploy phonebook'",
    "deploy:push": "git push heroku-phonebook main",
    "deploy": "npm run build:ui && npm run deploy:commit && npm run deploy:push",
    "start": "cd backend && npm run build:ui && npm run start"
  },
  "author": "Mykhailo Litvinov",
  "license": "MIT"
}
